<UserControl x:Class="DefaultViews.Views.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:SportBetting.WPF.Prism.Converters;assembly=SportBetting.WPF.Prism.Shared"
             xmlns:converters2="clr-namespace:SportBetting.WPF.Prism.Shared.Converters;assembly=SportBetting.WPF.Prism.Shared"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:l="clr-namespace:TranslationByMarkupExtension;assembly=TranslationByMarkupExtension"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:SportBetting.WPF.Prism.Shared.Models;assembly=SportBetting.WPF.Prism.Shared"
             xmlns:shared="clr-namespace:SportBetting.WPF.Prism.Shared;assembly=SportBetting.WPF.Prism.Shared"
             xmlns:viewModels="clr-namespace:ViewModels;assembly=ViewModels"
             xmlns:viewModels3="clr-namespace:ViewModels.ViewModels;assembly=ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels3:HeaderViewModel}"
             mc:Ignorable="d">

    <UserControl.Resources>
        <shared:BooleanToVisibilityCollapsedConverter x:Key="myBooleanToVisibilityCollapsedConverter" />
        <shared:MultiBiggerThanToBooleanConverter x:Key="myMultiBiggerThanToBooleanConverter" />
        <converters:MultiBooleanANDToVisibilityHiddenConverter x:Key="myMultyBooleanToVisibilityConverter" />
        <converters:MultiBooleanANDToVisibilityCollapsedConverter x:Key="myMultyBooleanToVisibilityCollapsedConverter" />
        <converters:MultiBooleanORToVisibilityConverter x:Key="myBooleanToVisibilityConverter" />
        <shared:InverseBooleanToVisibilityCollapsedConverter x:Key="myInverseBooleanToVisibilityConverter" />
        <shared:BiggerThanToBooleanConverter x:Key="BiggerThanToBooleanConverter" />
        <shared:SportDescriptorImageSelector x:Key="SportDescriptorToIconConverter" />

        <Style x:Key="searchRadioButton" TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Play" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="3"
                                      ShadowDepth="1"
                                      Color="#FFC3C3C3" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <DockPanel x:Name="dpnlContent"
                                   Width="{TemplateBinding Width}"
                                   LastChildFill="True">
                            <DockPanel.Background>
                                <ImageBrush ImageSource="{converters2:ResolveImagePath Path=BreadcrumbsButtonCenter.png}" />
                            </DockPanel.Background>
                            <DockPanel x:Name="dpnlLeft"
                                       Width="3"
                                       DockPanel.Dock="Left">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=BreadcrumbsLeft.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <DockPanel x:Name="dpnlRight"
                                       Width="3"
                                       DockPanel.Dock="Right">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=BreadcrumbsRight.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </DockPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="dpnlContent" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedBreadcrumbsButtonCenter.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlLeft" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedBreadcrumbsButtonLeft.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlRight" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedBreadcrumbsButtonRight.png}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="dpnlContent" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressButtonCenterOrange.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlLeft" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressButtonDownOrangeLeft.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlRight" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressButtonDownOrangeRight.png}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="true">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="3"
                                              ShadowDepth="1"
                                              Color="Black" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Storyboard x:Key="BasketBlink">
            <ObjectAnimationUsingKeyFrames Duration="0:0:1.5" Storyboard.TargetProperty="Background">
                <ObjectAnimationUsingKeyFrames.KeyFrames>
                    <DiscreteObjectKeyFrame KeyTime="0:0:0">
                        <DiscreteObjectKeyFrame.Value>
                            <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Highlited.png}" Stretch="Fill" />
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>

                    <DiscreteObjectKeyFrame KeyTime="0:0:0.3">
                        <DiscreteObjectKeyFrame.Value>
                            <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Box.png}" Stretch="Fill" />
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>

                    <DiscreteObjectKeyFrame KeyTime="0:0:0.6">
                        <DiscreteObjectKeyFrame.Value>
                            <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Highlited.png}" Stretch="Fill" />
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>

                    <DiscreteObjectKeyFrame KeyTime="0:0:0.9">
                        <DiscreteObjectKeyFrame.Value>
                            <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Box.png}" Stretch="Fill" />
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>

                    <DiscreteObjectKeyFrame KeyTime="0:0:1.2">
                        <DiscreteObjectKeyFrame.Value>
                            <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Highlited.png}" Stretch="Fill" />
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>

                    <DiscreteObjectKeyFrame KeyTime="0:0:1.5">
                        <DiscreteObjectKeyFrame.Value>
                            <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Box.png}" Stretch="Fill" />
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>

                </ObjectAnimationUsingKeyFrames.KeyFrames>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>

        <Style x:Key="WrapableTextBlock" TargetType="{x:Type TextBlock}">
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="MaxWidth" Value="120" />
            <Setter Property="Width" Value="120" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style x:Key="sportsToggleButton" TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Play" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <StackPanel Background="Transparent" Orientation="Vertical">
                            <Image x:Name="imgTopToggleButton"
                                   Width="86"
                                   Height="60"
                                   HorizontalAlignment="Center"
                                   Source="{converters2:ResolveImagePath Path=SportsIcon.png}" />
                            <Viewbox Width="80"
                                     Height="37"
                                     MaxWidth="80"
                                     MaxHeight="40"
                                     VerticalAlignment="Bottom"
                                     StretchDirection="DownOnly">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Viewbox>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ToggleButton.IsChecked" Value="true">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=SportsIconSelected.png}" />
                                <Setter Property="Foreground" Value="#FFFF4D00" />
                            </Trigger>
                            <Trigger Property="ToggleButton.IsEnabled" Value="False">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=SportsIconDisabled.png}" />
                                <Setter Property="Foreground" Value="#5A6164" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="true">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="3"
                                              ShadowDepth="1"
                                              Color="Black" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="liveToggleButton" TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Play" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <StackPanel Background="Transparent" Orientation="Vertical">
                            <Image x:Name="imgTopToggleButton"
                                   Width="86"
                                   Height="60"
                                   HorizontalAlignment="Center"
                                   Source="{converters2:ResolveImagePath Path=LiveIcon.png}" />
                            <Viewbox Width="80"
                                     Height="37"
                                     MaxWidth="80"
                                     MaxHeight="40"
                                     VerticalAlignment="Bottom"
                                     StretchDirection="DownOnly">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Viewbox>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ToggleButton.IsChecked" Value="true">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=LiveIconSelected.png}" />
                                <Setter Property="Foreground" Value="#FFFF4D00" />
                            </Trigger>
                            <Trigger Property="ToggleButton.IsEnabled" Value="False">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=LiveIconDisabled.png}" />
                                <Setter Property="Foreground" Value="#5A6164" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="true">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="3"
                                              ShadowDepth="1"
                                              Color="Black" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="resultsToggleButton" TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Play" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <StackPanel Background="Transparent" Orientation="Vertical">
                            <Image x:Name="imgTopToggleButton"
                                   Width="86"
                                   Height="60"
                                   HorizontalAlignment="Center"
                                   Source="{converters2:ResolveImagePath Path=ResultsIcon.png}" />
                            <Viewbox Width="80"
                                     Height="37"
                                     MaxWidth="80"
                                     MaxHeight="40"
                                     VerticalAlignment="Bottom"
                                     StretchDirection="DownOnly">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Viewbox>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ToggleButton.IsChecked" Value="true">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=ResultsIconSelected.png}" />
                                <Setter Property="Foreground" Value="#FFFF4D00" />
                            </Trigger>
                            <Trigger Property="ToggleButton.IsEnabled" Value="False">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=ResultsIconDisabled.png}" />
                                <Setter Property="Foreground" Value="#5A6164" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="true">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="3"
                                              ShadowDepth="1"
                                              Color="Black" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="virtualToggleButton" TargetType="{x:Type RadioButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="FontSize" Value="24" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="FontFamily" Value="Play" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <StackPanel Background="Transparent" Orientation="Vertical">
                            <Image x:Name="imgTopToggleButton"
                                   Width="86"
                                   Height="60"
                                   HorizontalAlignment="Center"
                                   Source="{converters2:ResolveImagePath Path=VirtualSportsIcon.png}" />
                            <Viewbox Width="80"
                                     Height="37"
                                     MaxWidth="80"
                                     MaxHeight="40"
                                     VerticalAlignment="Bottom"
                                     StretchDirection="DownOnly">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Bottom"
                                                  RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Viewbox>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="ToggleButton.IsChecked" Value="true">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=VirtualSportsIconSelected.png}" />
                                <Setter Property="Foreground" Value="#FFFF4D00" />
                            </Trigger>
                            <Trigger Property="ToggleButton.IsEnabled" Value="False">
                                <Setter TargetName="imgTopToggleButton" Property="Source" Value="{converters2:ResolveImagePath Path=VirtualSportsIconDisabled.png}" />
                                <Setter Property="Foreground" Value="#5A6164" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="ToggleButton.IsChecked" Value="true">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="3"
                                              ShadowDepth="1"
                                              Color="Black" />
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <viewModels:UserTemplateSelector x:Key="UserTemplateSelector" />
        <shared:UserToBooleanConverter x:Key="UserToBooleanConverter" />
        <shared:InverseUserToBooleanConverter x:Key="InverseUserToBooleanConverter" />
        <shared:InvertBooleanConverter x:Key="myInvertBooleanConverter" />
        <shared:MultistringTagConverter x:Key="MultistringTagConverter" />

        <Style x:Key="showHideButton"
               BasedOn="{StaticResource navigationButton}"
               TargetType="{x:Type Button}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="True">
                    <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="navigationButtonOrangeHeader" TargetType="{x:Type RadioButton}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="-1,0,0,0" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <DockPanel x:Name="dpnlContent"
                                   Width="{TemplateBinding Width}"
                                   Height="102"
                                   LastChildFill="True">
                            <DockPanel.Background>
                                <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonCenter.png}" />
                            </DockPanel.Background>

                            <DockPanel x:Name="dpnlLeft"
                                       Width="4"
                                       Height="102"
                                       DockPanel.Dock="Left">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonLeft.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <DockPanel x:Name="dpnlRight"
                                       Width="4"
                                       Height="102"
                                       DockPanel.Dock="Right">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonRight.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </DockPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="dpnlContent" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedHeaderButtonCenter.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlLeft" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedHeaderButtonLeft.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlRight" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedHeaderButtonRight.png}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="navigationOrangeHeader" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="-1,0,0,0" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <DockPanel x:Name="dpnlContent"
                                   Width="{TemplateBinding Width}"
                                   Height="102"
                                   LastChildFill="True">
                            <DockPanel.Background>
                                <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonCenter.png}" />
                            </DockPanel.Background>

                            <DockPanel x:Name="dpnlLeft"
                                       Width="4"
                                       Height="102"
                                       DockPanel.Dock="Left">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonLeft.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <DockPanel x:Name="dpnlRight"
                                       Width="4"
                                       Height="102"
                                       DockPanel.Dock="Right">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonRight.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </DockPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="dpnlContent" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedHeaderButtonCenter.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlLeft" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedHeaderButtonLeft.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlRight" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=PressedHeaderButtonRight.png}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="navigationButtonOrangeHeaderAndPressed"
               BasedOn="{StaticResource navigationButtonOrangeHeader}"
               TargetType="{x:Type RadioButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <DockPanel x:Name="dpnlContent"
                                   Width="{TemplateBinding Width}"
                                   Height="102"
                                   LastChildFill="True">
                            <DockPanel.Background>
                                <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonCenter.png}" />
                            </DockPanel.Background>

                            <DockPanel x:Name="dpnlLeft"
                                       Width="4"
                                       Height="102"
                                       DockPanel.Dock="Left">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonLeft.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <DockPanel x:Name="dpnlRight"
                                       Width="4"
                                       Height="102"
                                       DockPanel.Dock="Right">
                                <DockPanel.Background>
                                    <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonRight.png}" />
                                </DockPanel.Background>
                            </DockPanel>
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </DockPanel>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="true">
                                <Setter TargetName="dpnlContent" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonOrangeCenter.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlLeft" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonOrangeLeft.png}" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="dpnlRight" Property="Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=HeaderButtonOrangeRight.png}" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ShowHideText" TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Play" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="Regular" />
            <Setter Property="Foreground" Value="#FFEFEFEF" />
            <Setter Property="LineHeight" Value="13" />
            <Setter Property="LineStackingStrategy" Value="BlockLineHeight" />
            <Setter Property="TextAlignment" Value="Left" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowMenu}" Value="true">
                    <Setter Property="Text" Value="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_FORM_HIDE}}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding ShowMenu}" Value="false">
                    <Setter Property="Text" Value="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_FORM_SHOW}}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <UserControl.Background>
        <ImageBrush AlignmentX="Left"
                    AlignmentY="Top"
                    ImageSource="{converters2:ResolveImagePath Path=HeaderBackground.png}"
                    Stretch="None"
                    TileMode="Tile"
                    Viewport="0,0,1,120"
                    ViewportUnits="Absolute" />
    </UserControl.Background>

    <StackPanel Height="auto">
        <!--  header  -->
        <Grid x:Name="cnvHeader"
              Height="120"
              HorizontalAlignment="Stretch">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Background">
                        <Setter.Value>
                            <ImageBrush AlignmentX="Left"
                                        AlignmentY="Top"
                                        ImageSource="{converters2:ResolveImagePath Path=BackgroundTile.png}"
                                        Stretch="None"
                                        TileMode="Tile"
                                        Viewport="0,0,3,3"
                                        ViewportUnits="Absolute" />
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding StationRepository.IsTestMode}" Value="True">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <ImageBrush AlignmentX="Left"
                                                AlignmentY="Top"
                                                ImageSource="{converters2:ResolveImagePath Path=texture.png}"
                                                Stretch="None"
                                                TileMode="Tile"
                                                Viewport="0,0,3,3"
                                                ViewportUnits="Absolute" />
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />

            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Grid.ColumnSpan="4"
                        Width="330"
                        MaxWidth="411"
                        MaxHeight="108"
                        Orientation="Horizontal">
                <RadioButton x:Name="sportsToggleButton"
                             Height="97"
                             MaxWidth="86"
                             Command="{Binding ShowCategoriesViewCommand}"
                             Focusable="False"
                             GroupName="headerBtn"
                             IsChecked="{Binding ChangeTracker.SelectedSports}"
                             IsEnabled="{Binding ChangeTracker.CurrentUser,
                                                 Converter={StaticResource InverseUserToBooleanConverter},
                                                 ConverterParameter=OperatorUser}"
                             Style="{StaticResource sportsToggleButton}"
                             Visibility="{Binding StationRepository.IsPrematchEnabled,
                                                  Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <Viewbox Width="85"
                             Height="30"
                             MinWidth="85"
                             MinHeight="30"
                             MaxWidth="85"
                             MaxHeight="30"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Bottom"
                             StretchDirection="DownOnly">
                        <TextBlock Width="125"
                                   MaxWidth="125"
                                   Style="{StaticResource WrapableTextBlock}"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_SPORTS}}" />
                    </Viewbox>
                </RadioButton>
                <RadioButton x:Name="liveToggleButton"
                             Height="97"
                             MaxWidth="86"
                             Command="{Binding ShowLiveViewCommand}"
                             Focusable="False"
                             GroupName="headerBtn"
                             IsChecked="{Binding ChangeTracker.SelectedLive}"
                             IsEnabled="{Binding ChangeTracker.CurrentUser,
                                                 Converter={StaticResource InverseUserToBooleanConverter},
                                                 ConverterParameter=OperatorUser}"
                             Style="{StaticResource liveToggleButton}"
                             Visibility="{Binding StationRepository.IsLiveMatchEnabled,
                                                  Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <Viewbox Width="85"
                             Height="30"
                             MinWidth="85"
                             MinHeight="30"
                             MaxWidth="85"
                             MaxHeight="30"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Bottom"
                             StretchDirection="DownOnly">
                        <TextBlock Width="125"
                                   MaxWidth="125"
                                   Style="{StaticResource WrapableTextBlock}"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_FORM_LIVE_BETS}}" />
                    </Viewbox>
                </RadioButton>
                <RadioButton x:Name="resultsToggleButton"
                             Height="97"
                             MaxWidth="86"
                             Command="{Binding ShowResultsViewCommand}"
                             Focusable="False"
                             GroupName="headerBtn"
                             IsChecked="{Binding ChangeTracker.SelectedResults}"
                             IsEnabled="{Binding ChangeTracker.CurrentUser,
                                                 Converter={StaticResource InverseUserToBooleanConverter},
                                                 ConverterParameter=OperatorUser}"
                             Style="{StaticResource resultsToggleButton}"
                             Visibility="{Binding StationRepository.ResultsVisible,
                                                  Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <Viewbox Width="85"
                             Height="30"
                             MinWidth="85"
                             MinHeight="30"
                             MaxWidth="85"
                             MaxHeight="30"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Bottom"
                             StretchDirection="DownOnly">
                        <TextBlock Width="125"
                                   MaxWidth="125"
                                   Style="{StaticResource WrapableTextBlock}"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS}}" />
                    </Viewbox>
                </RadioButton>
                <RadioButton x:Name="virtualToggleButton"
                             Height="97"
                             MaxWidth="86"
                             Command="{Binding ShowEntertainmentCommand}"
                             Focusable="False"
                             GroupName="headerBtn"
                             IsChecked="{Binding ChangeTracker.SelectedVirtualSports}"
                             IsEnabled="{Binding ChangeTracker.CurrentUser,
                                                 Converter={StaticResource InverseUserToBooleanConverter},
                                                 ConverterParameter=OperatorUser}"
                             Style="{StaticResource virtualToggleButton}">
                    <RadioButton.Visibility>
                        <MultiBinding Converter="{StaticResource myBooleanToVisibilityConverter}">
                            <Binding Path="StationRepository.AllowVfl" />
                            <Binding Path="StationRepository.AllowVhc" />
                        </MultiBinding>
                    </RadioButton.Visibility>

                    <Viewbox Width="85"
                             Height="40"
                             MinWidth="85"
                             MinHeight="40"
                             MaxWidth="85"
                             MaxHeight="40"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Bottom"
                             StretchDirection="DownOnly">
                        <TextBlock Width="125"
                                   MaxWidth="125"
                                   Style="{StaticResource WrapableTextBlock}"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_VIRTUAL_SPORTS}}" />
                    </Viewbox>
                </RadioButton>

            </StackPanel>
            <!--  </Viewbox>  -->
            <!--  login  -->
            <Label Name="UserControl"
                   Grid.Column="4"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   HorizontalContentAlignment="Stretch"
                   Content="{Binding ChangeTracker.CurrentUser}"
                   ContentTemplateSelector="{StaticResource UserTemplateSelector}" />

            <!--  insert credit  -->
            <Grid Grid.Column="5"
                  Width="68"
                  Height="102"
                  HorizontalAlignment="Right">
                <RadioButton x:Name="BtnInsCrdt"
                             Width="68"
                             Height="102"
                             AutomationProperties.AutomationId="InsertTicket"
                             Command="{Binding InsertCreditNoteCommand}"
                             FontSize="20"
                             FontWeight="Bold"
                             Foreground="#FFC0C0C0"
                             GroupName="headerBtn"
                             IsChecked="{Binding ChangeTracker.SelectedTicket}"
                             IsEnabled="{Binding ChangeTracker.CurrentUser,
                                                 Converter={StaticResource InverseUserToBooleanConverter},
                                                 ConverterParameter=OperatorUser}"
                             Style="{StaticResource navigationButtonOrangeHeaderAndPressed}">
                    <DockPanel Width="60" Height="102">
                        <Image Width="48"
                               Height="26"
                               Margin="0,15,0,0"
                               DockPanel.Dock="Top"
                               Source="{converters2:ResolveImagePath Path=InsertCNicon.png}" />

                        <TextBlock Margin="0,0,0,15"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Bottom"
                                   DockPanel.Dock="Bottom"
                                   FontFamily="Play"
                                   FontSize="14"
                                   Foreground="White"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_INSERT_TICKET}}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap" />
                    </DockPanel>
                </RadioButton>

                <Grid Name="grdClosed" Visibility="{Binding ElementName=BtnInsCrdt, Path=IsEnabled, Converter={StaticResource myInverseBooleanToVisibilityConverter}}">
                    <Grid.Background>
                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=SmallDisabled_row.png}" />
                    </Grid.Background>
                </Grid>
            </Grid>
            <Button x:Name="btnCashOut"
                    Grid.Column="6"
                    Width="68"
                    Height="102"
                    Margin="3, 0, 0 , 0"
                    HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="CashOut"
                    Command="{Binding PrintCreditNoteCommand}"
                    Focusable="False"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="#FFC0C0C0"
                    Style="{StaticResource navigationOrangeHeader}">
                <Button.Visibility>
                    <MultiBinding Converter="{StaticResource myMultyBooleanToVisibilityConverter}">
                        <MultiBinding.Bindings>
                            <Binding Converter="{StaticResource UserToBooleanConverter}"
                                     ConverterParameter="AnonymousUser"
                                     Path="ChangeTracker.CurrentUser" />
                            <Binding Path="StationRepository.AllowAnonymousBetting" />
                            <Binding Converter="{StaticResource BiggerThanToBooleanConverter}"
                                     ConverterParameter="0"
                                     Path="ChangeTracker.CurrentUser.Cashpool" />
                            <Binding Converter="{StaticResource myInvertBooleanConverter}" Path="StationRepository.IsTestMode" />
                        </MultiBinding.Bindings>
                    </MultiBinding>
                </Button.Visibility>
                <DockPanel Width="60"
                           Height="102"
                           HorizontalAlignment="Center">
                    <Image Width="68"
                           Height="35"
                           Margin="0,15,0,0"
                           DockPanel.Dock="Top"
                           Source="{converters2:ResolveImagePath Path=CashOutIcon.png}" />
                    <TextBlock Margin="0,0,0,15"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Bottom"
                               DockPanel.Dock="Bottom"
                               FontFamily="Play"
                               FontSize="14"
                               Foreground="White"
                               Text="{l:Translate Key={x:Static l:MultistringTags.CASH_OUT}}"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />
                </DockPanel>
            </Button>
            <!--  testMode section  -->
            <Grid Grid.Column="7"
                  Width="250"
                  Height="102"
                  Margin="5, 0, 0, 0"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Center"
                  Visibility="{Binding StationRepository.IsTestMode,
                                       Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                <Grid.Background>
                    <ImageBrush AlignmentX="Left"
                                AlignmentY="Top"
                                ImageSource="{converters2:ResolveImagePath Path=BackgroundBets_Test.png}"
                                Stretch="Fill" />
                </Grid.Background>

                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontFamily="Play"
                           FontSize="30"
                           FontWeight="Bold"
                           IsHyphenationEnabled="True"
                           LineHeight="30"
                           LineStackingStrategy="BlockLineHeight"
                           Text="TEST MODE"
                           TextAlignment="Center"
                           TextWrapping="Wrap" />

            </Grid>

            <Button x:Name="btnExitTextMode"
                    Grid.Column="8"
                    Width="68"
                    Height="102"
                    HorizontalAlignment="Left"
                    AutomationProperties.AutomationId="ExitTextMode"
                    Command="{Binding ExitTestMode}"
                    Focusable="False"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="#FFC0C0C0"
                    Style="{StaticResource navigationOrangeHeader}"
                    Visibility="{Binding StationRepository.IsTestMode,
                                         Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                <DockPanel Width="60" Height="102">
                    <Image Width="68"
                           Height="35"
                           Margin="0,10,0,0"
                           DockPanel.Dock="Top"
                           Source="{converters2:ResolveImagePath Path=exit_white.png}" />
                    <Viewbox StretchDirection="DownOnly">
                        <TextBlock DockPanel.Dock="Bottom"
                                   FontFamily="Play"
                                   FontSize="14"
                                   Foreground="White"
                                   Text="Exit"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap" />
                    </Viewbox>
                </DockPanel>
            </Button>
          
            <!--  place bets  -->
            <Grid Grid.Column="9"
                  Width="250"
                  Height="102"
                  HorizontalAlignment="Right">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseUp">
                        <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type StackPanel}}, Path=DataContext.OpenPlaceBetCommand}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <Grid.Style>
                    <Style>
                        <Setter Property="Grid.Background">
                            <Setter.Value>
                                <ImageBrush ImageSource="{converters2:ResolveImagePath Path=Basket_Box.png}" Stretch="Fill" />
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding TicketHandler.Count}" Value="0">
                                <Setter Property="Grid.Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=BackgroundBets.png}" Stretch="Fill" />
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                <Setter Property="Grid.Background">
                                    <Setter.Value>
                                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=red_button.png}" Stretch="Fill" />
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ChangeTracker.BetSelected}" Value="true">
                                <DataTrigger.EnterActions>
                                    <!--  <RemoveStoryboard BeginStoryboardName="BeginBasketBlink" />  -->
                                    <BeginStoryboard x:Name="BeginBasketBlink" Storyboard="{StaticResource BasketBlink}" />
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ChangeTracker.BetSelected}" Value="false">
                                <DataTrigger.EnterActions>
                                    <RemoveStoryboard BeginStoryboardName="BeginBasketBlink" />
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>

                </Grid.Style>
                <Canvas Width="250" Height="102">

                    <!--  previous canvas horizontal location value: Canvas.Left="710"  -->
                    <Canvas Canvas.Left="9"
                            Canvas.Top="5"
                            Width="230"
                            Height="75">
                        <Canvas.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="False">
                                        <Setter Property="Canvas.Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Canvas.Style>
                        <Viewbox Canvas.Left="0"
                                 Canvas.Top="1"
                                 StretchDirection="DownOnly">
                            <TextBlock FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_NUMBER_BETS}}"
                                       TextAlignment="Left"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TicketHandler.TicketState}" Value="MultySingles">
                                                <Setter Property="Grid.Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Viewbox>
                        <Viewbox Canvas.Left="0"
                                 Canvas.Top="23"
                                 StretchDirection="DownOnly">
                            <TextBlock FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_TOTAL_ODD}}"
                                       TextAlignment="Left"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TicketHandler.TicketsInBasket.Count, Converter={StaticResource BiggerThanToBooleanConverter}, ConverterParameter=1}" Value="True">
                                                <Setter Property="Grid.Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Viewbox>
                        <Viewbox Canvas.Left="0"
                                 Canvas.Top="46"
                                 StretchDirection="DownOnly">
                            <TextBlock FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_PRINT_STAKE_TOTAL}}"
                                       TextAlignment="Left"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Viewbox>
                        <Line Canvas.Left="0"
                              Canvas.Top="55"
                              Margin="0,10,0,0"
                              Stroke="White"
                              StrokeThickness="1"
                              X1="0"
                              X2="230"
                              Y1="0" />
                        <Viewbox Canvas.Left="0"
                                 Canvas.Top="69"
                                 StretchDirection="DownOnly">
                            <TextBlock FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_POSSWIN}}"
                                       TextAlignment="Left"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                            <DataTrigger>
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource myMultiBiggerThanToBooleanConverter}" ConverterParameter="0">
                                                        <MultiBinding.Bindings>
                                                            <Binding Path="TicketHandler.Count" />
                                                            <Binding Path="TicketHandler.Stake" />
                                                        </MultiBinding.Bindings>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <DataTrigger.Value>True</DataTrigger.Value>
                                                <Setter Property="Foreground" Value="#FFFF4E00" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Viewbox>
                        <DockPanel Canvas.Top="1" Canvas.Right="0">
                            <TextBlock AutomationProperties.AutomationId="NumOfBets"
                                       FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{Binding TicketHandler.Count}"
                                       TextAlignment="Right"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                        </DockPanel>
                        <DockPanel Canvas.Top="23" Canvas.Right="0">
                            <DockPanel.Style>
                                <Style>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding TicketHandler.TicketsInBasket.Count, Converter={StaticResource BiggerThanToBooleanConverter}, ConverterParameter=1}" Value="True">
                                            <Setter Property="Grid.Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DockPanel.Style>
                            <TextBlock AutomationProperties.AutomationId="TotoalOdds"
                                       FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{Binding TicketHandler.TotalOddDisplay,
                                                      StringFormat=\{0:N2\}}"
                                       TextAlignment="Right"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DockPanel>
                        <DockPanel Canvas.Top="46" Canvas.Right="0">
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock AutomationProperties.AutomationId="TotalStake"
                                           FontFamily="Play"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           IsHyphenationEnabled="True"
                                           LineHeight="16"
                                           LineStackingStrategy="BlockLineHeight"
                                           Text="{Binding TicketHandler.Stake,
                                                          StringFormat=\{0:N2\}}"
                                           TextAlignment="Right"
                                           TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#FFFFFFFF" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                    <Setter Property="Foreground" Value="White" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Viewbox>
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock Margin="3,0,0,0"
                                           FontFamily="Play"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           IsHyphenationEnabled="True"
                                           LineHeight="16"
                                           LineStackingStrategy="BlockLineHeight"
                                           Text="{Binding StationRepository.Currency}"
                                           TextAlignment="Right"
                                           TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#FFFFFFFF" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                    <Setter Property="Foreground" Value="White" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Viewbox>
                        </DockPanel>
                        <DockPanel Canvas.Top="69" Canvas.Right="0">
                            <TextBlock AutomationProperties.AutomationId="PossibleWin"
                                       FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{Binding TicketHandler.CurrentTicketPossibleWin,
                                                      StringFormat=\{0:N2\}}"
                                       TextAlignment="Right"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                            <DataTrigger>
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource myMultiBiggerThanToBooleanConverter}" ConverterParameter="0">
                                                        <MultiBinding.Bindings>
                                                            <Binding Path="TicketHandler.Count" />
                                                            <Binding Path="TicketHandler.Stake" />
                                                        </MultiBinding.Bindings>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <DataTrigger.Value>True</DataTrigger.Value>
                                                <Setter Property="Foreground" Value="#FFFF4E00" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Margin="3,0,0,0"
                                       FontFamily="Play"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       IsHyphenationEnabled="True"
                                       LineHeight="16"
                                       LineStackingStrategy="BlockLineHeight"
                                       Text="{Binding StationRepository.Currency}"
                                       TextAlignment="Right"
                                       TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding StationRepository.Active}" Value="0">
                                                <Setter Property="Foreground" Value="White" />
                                            </DataTrigger>
                                            <DataTrigger>
                                                <DataTrigger.Binding>
                                                    <MultiBinding Converter="{StaticResource myMultiBiggerThanToBooleanConverter}" ConverterParameter="0">
                                                        <MultiBinding.Bindings>
                                                            <Binding Path="TicketHandler.Count" />
                                                            <Binding Path="TicketHandler.Stake" />
                                                        </MultiBinding.Bindings>
                                                    </MultiBinding>
                                                </DataTrigger.Binding>
                                                <DataTrigger.Value>True</DataTrigger.Value>
                                                <Setter Property="Foreground" Value="#FFFF4E00" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DockPanel>
                    </Canvas>
                    <Canvas Canvas.Left="15"
                            Canvas.Top="5"
                            Width="230"
                            Height="75"
                            Background="Black"
                            Visibility="Collapsed">
                        <Canvas.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="True">
                                        <Setter Property="Canvas.Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Canvas.Style>
                    </Canvas>
                </Canvas>

                <Label Width="250" Height="102" Name="LockedStatusLabel" VerticalAlignment="Center">
                    <Label.Background>
                        <ImageBrush AlignmentX="Left"
                                AlignmentY="Top"
                                ImageSource="{converters2:ResolveImagePath Path=BackgroundBets.png}"
                                Stretch="Fill" />
                    </Label.Background>
                    <DockPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="234" Height="96">
                        <Image Source="{converters2:ResolveImagePath Path=WarningIconSmall.png}" VerticalAlignment="Center" Width="41" Height="37" DockPanel.Dock="Left" Margin="20,0,0,0"></Image>
                        <TextBlock DockPanel.Dock="Left" TextWrapping="Wrap" Foreground="White" FontSize="20" FontWeight="Bold" TextAlignment="Center" Margin="10,0,20,0" VerticalAlignment="Center" HorizontalAlignment="Left" Text="{Binding LockText}"></TextBlock>
                    </DockPanel>
                    <Label.Visibility>
                        <MultiBinding Converter="{StaticResource myMultyBooleanToVisibilityCollapsedConverter}">
                            <MultiBinding.Bindings>
                                <Binding Path="IsStationLocked"></Binding>
                                <Binding Converter="{StaticResource UserToBooleanConverter}"
                                     ConverterParameter="OperatorUser"
                                     Path="ChangeTracker.CurrentUser" />
                            </MultiBinding.Bindings>
                        </MultiBinding>
                    </Label.Visibility>
                </Label>
            </Grid>

            <Grid x:Name="grdTicketBasket"
                  Grid.Column="10"
                  Width="68"
                  Height="102"
                  Margin="0, 0, 5, 0">
                <!--
                    <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseUp">
                    <catel:EventToCommand Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type StackPanel}}, Path=DataContext.OpenPlaceBetCommand}" />
                    </i:EventTrigger>
                    </i:Interaction.Triggers>
                -->
                <Button Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type StackPanel}},
                                          Path=DataContext.OpenPlaceBetCommand}"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="White"
                        Style="{StaticResource navigationButtonTicket}">
                    <DockPanel Width="60"
                               Height="102"
                               Margin="0,-5,0,0">
                        <Image Width="75"
                               Height="40"
                               Margin="0,20,0,0"
                               HorizontalAlignment="Center"
                               DockPanel.Dock="Top"
                               Source="{converters2:ResolveImagePath Path=TicketIcon.png}" />

                        <TextBlock Margin="0,10,0,0"
                                   DockPanel.Dock="Bottom"
                                   FontFamily="Play"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="White"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_TICKET}}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap" />


                    </DockPanel>


                </Button>
                <Grid Name="grdClosedTicket" Visibility="{Binding ElementName=BtnInsCrdt, Path=IsEnabled, Converter={StaticResource myInverseBooleanToVisibilityConverter}}">
                    <Grid.Background>
                        <ImageBrush ImageSource="{converters2:ResolveImagePath Path=SmallDisabled_row.png}" />
                    </Grid.Background>
                </Grid>
            </Grid>
        </Grid>

        <!--  breadcrumbs  -->

        <Grid Height="60"
              HorizontalAlignment="Stretch"
              Visibility="{Binding ChangeTracker.HeaderVisible,
                                   Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
            <Grid.Background>
                <ImageBrush ImageSource="{converters2:ResolveImagePath Path=BreadcrumbsCenter.png}" />
            </Grid.Background>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="2" HorizontalAlignment="Right">
                <Image Width="4"
                       Height="60"
                       Margin="0,0,-1,0"
                       Source="{converters2:ResolveImagePath Path=BreadcrumbsRight.png}" />
                <StackPanel.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="True">
                                <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
            </StackPanel>

            <!--  navigation and notifications  -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">

                <Button Width="65"
                        Height="60"
                        Command="{Binding ShowMenuCommand}"
                        CommandParameter="1"
                        DockPanel.Dock="Left"
                        Style="{StaticResource showHideButton}">
                    <StackPanel>
                        <Image Width="24"
                               Height="20"
                               Margin="5, 0, 5, 5"
                               DockPanel.Dock="Top"
                               Source="{converters2:ResolveImagePath Path=close_icon01.png}" />
                        <TextBlock Margin="0, 4, 0, 0"
                                   HorizontalAlignment="Center"
                                   Style="{StaticResource ShowHideText}" />
                    </StackPanel>

                </Button>

                <Button x:Name="btnPrev"
                        Width="60"
                        Height="60"
                        AutomationProperties.AutomationId="Back"
                        Command="{Binding PrevView}"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#FFC0C0C0"
                        Style="{StaticResource navigationButton}"
                        Visibility="{Binding StationRepository.IsTestMode,
                                             Converter={StaticResource myInverseBooleanToVisibilityConverter}}">
                    <Image Width="22"
                           Height="29"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Source="{converters2:ResolveImagePath Path=BackActive.png}" />
                </Button>
                <Button x:Name="btnNext"
                        Width="60"
                        Height="60"
                        AutomationProperties.AutomationId="Forward"
                        Command="{Binding NextView}"
                        FontSize="20"
                        FontWeight="Bold"
                        Foreground="#FFC0C0C0"
                        Style="{StaticResource navigationButtonOrange}"
                        Visibility="{Binding StationRepository.IsTestMode,
                                             Converter={StaticResource myInverseBooleanToVisibilityConverter}}">
                    <Image Width="22"
                           Height="29"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Right"
                           Source="{converters2:ResolveImagePath Path=FwdActive.png}" />
                </Button>

                <StackPanel>
                    <Image Width="4"
                           Height="60"
                           Margin="-1,0,0,0"
                           HorizontalAlignment="Left"
                           Source="{converters2:ResolveImagePath Path=BreadcrumbsLeft.png}" />
                    <StackPanel.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="True">
                                    <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                </StackPanel>


                <StackPanel Margin="10"
                            VerticalAlignment="Center"
                            DockPanel.Dock="Left"
                            Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style x:Key="NormalHiddenText"
                               BasedOn="{StaticResource normalText}"
                               TargetType="{x:Type TextBlock}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChangeTracker.AdminTitle2}" Value="{x:Null}">
                                    <Setter Property="TextBlock.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>
                    <StackPanel.Style>
                        <Style>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="True">
                                    <Setter Property="StackPanel.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Style="{StaticResource normalText}" Text="{Binding ChangeTracker.AdminTitle1, Converter={StaticResource MultistringTagConverter}}" />
                    <TextBlock Margin="5,0,5,0"
                               Style="{StaticResource NormalHiddenText}"
                               Text="/" />
                    <TextBlock Style="{StaticResource normalText}" Text="{Binding ChangeTracker.AdminTitle2, Converter={StaticResource MultistringTagConverter}}" />

                </StackPanel>
            </StackPanel>

            <Grid x:Name="spnlLeftButtons"
                  Grid.Column="1"
                  HorizontalAlignment="Stretch"
                  Visibility="{Binding ChangeTracker.IsBetdomainViewOpen,
                                       Converter={StaticResource myInverseBooleanToVisibilityConverter}}">
                <Grid.Style>
                    <Style>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="False">
                                <Setter Property="Canvas.Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Image Width="4"
                       Height="60"
                       Margin="-1,0,0,0"
                       HorizontalAlignment="Left"
                       Source="{converters2:ResolveImagePath Path=BreadcrumbsLeft.png}" />

                <Image Width="4"
                       Height="60"
                       Margin="-1,0,0,0"
                       HorizontalAlignment="Right"
                       Source="{converters2:ResolveImagePath Path=BreadcrumbsRight.png}" />

                <!--  live match buttons  -->
                <Grid Name="LiveMatchFilters"
                      HorizontalAlignment="Stretch"
                      Visibility="{Binding ChangeTracker.SelectedLive,
                                           Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <UniformGrid HorizontalAlignment="Stretch" Columns="3">
                        <RadioButton Grid.Column="0"
                                     Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="livebysport"
                                     Command="{Binding LiveModeCommand}"
                                     CommandParameter="0"
                                     GroupName="LiveTimeFilterButtons"
                                     IsChecked="{Binding LiveBySportChecked}"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Image Width="34"
                                           Height="34"
                                           VerticalAlignment="Center"
                                           Source="{converters2:ResolveImagePath Path=World.png}" />
                                    <TextBlock Margin="9,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="23"
                                               FontWeight="Bold"
                                               Foreground="#fff"
                                               Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_LIVE_BY_SPORT}}" />
                                </StackPanel>
                            </Viewbox>
                        </RadioButton>

                        <RadioButton Grid.Column="0"
                                     Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="topgameslive"
                                     Command="{Binding LiveModeCommand}"
                                     CommandParameter="4"
                                     GroupName="LiveTimeFilterButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Image Width="34"
                                           Height="34"
                                           VerticalAlignment="Center"
                                           Source="{converters2:ResolveImagePath Path=Top.png}" />
                                    <TextBlock Margin="9,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="23"
                                               FontWeight="Bold"
                                               Foreground="#fff"
                                               Text="{l:Translate Key={x:Static l:MultistringTags.TOP_GAMES}}" />
                                </StackPanel>
                            </Viewbox>
                        </RadioButton>

                        <RadioButton Grid.Column="0"
                                     Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="livetimetable"
                                     Command="{Binding LiveModeCommand}"
                                     CommandParameter="3"
                                     GroupName="LiveTimeFilterButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                    <Image Width="34"
                                           Height="34"
                                           VerticalAlignment="Center"
                                           Source="{converters2:ResolveImagePath Path=Live.png}" />
                                    <TextBlock Margin="9,0,0,0"
                                               VerticalAlignment="Center"
                                               FontSize="23"
                                               FontWeight="Bold"
                                               Foreground="#fff"
                                               Text="{l:Translate Key={x:Static l:MultistringTags.LIVE_TIMETABLE}}" />
                                </StackPanel>
                            </Viewbox>
                        </RadioButton>
                    </UniformGrid>
                </Grid>

                <!--  match filters  -->
                <Grid Name="MatchFilters"
                      HorizontalAlignment="Stretch"
                      Visibility="{Binding ChangeTracker.SelectedSports,
                                           Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="31*" />
                        <ColumnDefinition Width="21*" />
                        <ColumnDefinition Width="24*" />
                        <ColumnDefinition Width="22*" />
                    </Grid.ColumnDefinitions>
                    <RadioButton Grid.Column="0"
                                 Height="60"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="toptournaments"
                                 Command="{Binding TopTournamentsCommand}"
                                 GroupName="MatchFilterButtons"
                                 IsChecked="{Binding TopTournamentsChecked}"
                                 Style="{StaticResource sportBarRadioButton}">
                        <Viewbox StretchDirection="DownOnly">
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                <Image Width="34"
                                       Height="34"
                                       VerticalAlignment="Center"
                                       Source="{converters2:ResolveImagePath Path=Top.png}" />
                                <TextBlock Margin="9,0,0,0"
                                           VerticalAlignment="Center"
                                           FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_TOP_TOURNAMENTS}}" />
                            </StackPanel>
                        </Viewbox>
                    </RadioButton>

                    <RadioButton Grid.Column="1"
                                 Height="60"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="allsports"
                                 Command="{Binding AllSportsCommand}"
                                 GroupName="MatchFilterButtons"
                                 IsChecked="False"
                                 Style="{StaticResource sportBarRadioButton}">
                        <Viewbox StretchDirection="DownOnly">
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                <Image Width="34"
                                       Height="34"
                                       VerticalAlignment="Center"
                                       Source="{converters2:ResolveImagePath Path=World.png}" />
                                <TextBlock Margin="9,0,0,0"
                                           VerticalAlignment="Center"
                                           FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.ALL_SPORTS}}" />
                            </StackPanel>
                        </Viewbox>
                    </RadioButton>

                    <RadioButton Grid.Column="2"
                                 Height="60"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="todaysoffer"
                                 Command="{Binding TodaysOfferCommand}"
                                 GroupName="MatchFilterButtons"
                                 IsChecked="False"
                                 Style="{StaticResource sportBarRadioButton}">
                        <Viewbox StretchDirection="DownOnly">
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                <Image Width="34"
                                       Height="34"
                                       VerticalAlignment="Center"
                                       Source="{converters2:ResolveImagePath Path=Today.png}" />
                                <TextBlock Margin="9,0,0,0"
                                           VerticalAlignment="Center"
                                           FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_OFFER_TODAY}}" />
                            </StackPanel>
                        </Viewbox>
                    </RadioButton>

                    <RadioButton Grid.Column="3"
                                 Height="60"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="lastminute"
                                 Command="{Binding LastMinuteCommand}"
                                 GroupName="MatchFilterButtons"
                                 IsChecked="False"
                                 Style="{StaticResource sportBarRadioButton}">
                        <Viewbox StretchDirection="DownOnly">
                            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                                <Image Width="34"
                                       Height="34"
                                       VerticalAlignment="Center"
                                       Source="{converters2:ResolveImagePath Path=LastMinute.png}" />
                                <TextBlock Margin="9,0,0,0"
                                           VerticalAlignment="Center"
                                           FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_LASTMINUTE}}" />
                            </StackPanel>
                        </Viewbox>
                    </RadioButton>
                </Grid>

                <!--  results buttons  -->
                <Grid HorizontalAlignment="Stretch" Visibility="{Binding ShowResultFilters, Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="15*" />
                        <ColumnDefinition Width="15*" />
                        <ColumnDefinition Width="70*" />
                    </Grid.ColumnDefinitions>

                    <RadioButton Grid.Column="0"
                                 Height="60"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="resultstoday"
                                 Command="{Binding ResultsTimeCommand}"
                                 CommandParameter="0"
                                 GroupName="resultsBarlButtons"
                                 IsChecked="{Binding TodayResultsChecked}"
                                 Style="{StaticResource sportBarRadioButton}">
                        <Viewbox StretchDirection="DownOnly">
                            <TextBlock FontSize="23"
                                       FontWeight="Bold"
                                       Foreground="#fff"
                                       Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_TODAY}}" />
                        </Viewbox>
                    </RadioButton>

                    <RadioButton Grid.Column="1"
                                 Height="60"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Center"
                                 AutomationProperties.AutomationId="resultstesretday"
                                 Command="{Binding ResultsTimeCommand}"
                                 CommandParameter="1"
                                 GroupName="resultsBarlButtons"
                                 Style="{StaticResource sportBarRadioButton}">
                        <Viewbox StretchDirection="DownOnly">
                            <TextBlock FontSize="23"
                                       FontWeight="Bold"
                                       Foreground="#fff"
                                       Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_YESTERDAY}}" />
                        </Viewbox>
                    </RadioButton>

                    <UniformGrid Grid.Column="2" Columns="5">
                        <RadioButton Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="resultsminus2"
                                     Command="{Binding ResultsTimeCommand}"
                                     CommandParameter="2"
                                     GroupName="resultsBarlButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_MINUS_X_DAYS},
                                                              Args=2}" />
                            </Viewbox>
                        </RadioButton>

                        <RadioButton Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="resultsminus3"
                                     Command="{Binding ResultsTimeCommand}"
                                     CommandParameter="3"
                                     GroupName="resultsBarlButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_MINUS_X_DAYS},
                                                              Args=3}" />
                            </Viewbox>
                        </RadioButton>

                        <RadioButton Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="resultsminus4"
                                     Command="{Binding ResultsTimeCommand}"
                                     CommandParameter="4"
                                     GroupName="resultsBarlButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_MINUS_X_DAYS},
                                                              Args=4}" />
                            </Viewbox>
                        </RadioButton>

                        <RadioButton Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="resultsminus5"
                                     Command="{Binding ResultsTimeCommand}"
                                     CommandParameter="5"
                                     GroupName="resultsBarlButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_MINUS_X_DAYS},
                                                              Args=5}" />
                            </Viewbox>
                        </RadioButton>

                        <RadioButton Height="60"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"
                                     AutomationProperties.AutomationId="resultsminus6"
                                     Command="{Binding ResultsTimeCommand}"
                                     CommandParameter="6"
                                     GroupName="resultsBarlButtons"
                                     Style="{StaticResource sportBarRadioButton}">
                            <Viewbox StretchDirection="DownOnly">
                                <TextBlock FontSize="23"
                                           FontWeight="Bold"
                                           Foreground="#fff"
                                           Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESULTS_MINUS_X_DAYS},
                                                              Args=6}" />
                            </Viewbox>
                        </RadioButton>
                    </UniformGrid>
                </Grid>
            </Grid>


            <!--  right navigation control (search...)  -->
            <StackPanel x:Name="spnlRightButtons"
                        Grid.Column="2"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                <RadioButton Width="60"
                             Height="60"
                             Margin="0,0,0,0"
                             AutomationProperties.AutomationId="Search"
                             Command="{Binding DoSearchCommand}"
                             GroupName="headerBtn"
                             Style="{StaticResource searchRadioButton}"
                             Visibility="{Binding ChangeTracker.SearchVisibility}">
                    <Viewbox StretchDirection="DownOnly">
                        <Image Width="34"
                               Height="34"
                               Source="{converters2:ResolveImagePath Path=SearchIcon.png}" />
                    </Viewbox>
                </RadioButton>

                <Button Width="200"
                        Height="60"
                        AutomationProperties.AutomationId="RestoreTicket"
                        Command="{Binding RestoreTicketCommand}"
                        Focusable="False"
                        Visibility="{Binding StationRepository.IsStoreTicketEnabled,
                                             Converter={StaticResource myBooleanToVisibilityCollapsedConverter}}">
                    <Button.Style>
                        <Style BasedOn="{StaticResource navigationButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ChangeTracker.CurrentUser, Converter={StaticResource InverseUserToBooleanConverter}, ConverterParameter=OperatorUser}" Value="False">
                                    <Setter Property="Canvas.Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Viewbox StretchDirection="DownOnly">
                        <TextBlock Margin="10,0,5,0"
                                   Focusable="False"
                                   FontFamily="Play"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#FFC0C0C0"
                                   Text="{l:Translate Key={x:Static l:MultistringTags.TERMINAL_RESTORE_TICKET}}"
                                   TextAlignment="Left"
                                   TextWrapping="Wrap" />
                    </Viewbox>
                </Button>
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
